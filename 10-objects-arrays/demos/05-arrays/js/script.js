function setup() {
  createCanvas(400, 400);
  noLoop();
}

function draw() {
  // we create an array with colours
  const colourList = ["#FF0000", "#00FF00", "#0000FF"];
  console.log(colourList);
}
