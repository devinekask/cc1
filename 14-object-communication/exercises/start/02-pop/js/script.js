/*
Generate 50 bubbles with random positions and sizes. 
When the user clicks, the bubble should disappear.

Hint: You can use the dist() function to check if the mouse is inside the bubble. Check the demos folder for an example.
*/

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(0);
}
